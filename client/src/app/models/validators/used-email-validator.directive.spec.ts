import { UsedEmailValidatorDirective } from './used-email-validator.directive';

describe('UsedEmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UsedEmailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
